{
  "name": "naman-personal-website",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev --host --port 3000",
    "start": "astro dev --host --port 3000",
    "build": "astro build",
    "preview": "astro preview --host --port 3000"
  },
  "dependencies": {
    "astro": "^5.7.1"
  }
}
